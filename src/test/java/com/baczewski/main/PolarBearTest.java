package com.baczewski.main;

import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

import java.time.LocalDateTime;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.spy;
import static org.mockito.Mockito.when;

class PolarBearTest {

/*    @Test
    public void shouldBeAliveAfterCreation() {
        // given
        Animal animal= new PolarBear();
        // when

        // then
        assertTrue(animal.isAlive());
    }

    @Test
    public void shouldBeAliveAfterEat() {
        // given
        Animal animal = new PolarBear();
        // when
        animal.eat();
        // then
        assertTrue(animal.isAlive());
    }

    @Test
    public void shouldBeDeadIfEat11DaysBefore() {
        // given
        Animal animal= spy(new PolarBear());
        // when
        LocalDateTime value = LocalDateTime.now().minusDays(11);
        when(animal.getLastEatTime()).thenReturn(value);
        // then
        assertFalse(animal.isAlive());
    }
    
    @Test
    public void shouldReturnWeight200() {
        // given
        Animal animal = new PolarBear();
        // when

        // then
        assertEquals(200,animal.getWeight());
    }*/

}